{
  "name": "video-caller",
  "version": "0.0.1",
  "scripts": {
    "dev:server": "uv run cargo run",
    "dev:client": "bun run  --filter '*client' dev",
    "format": "biome format --write",
    "lint": "biome lint",
    "lint:fix": "biome lint --write"
  },
  "workspaces": [
    "client"
  ],
  "packageManager": "bun@1.3.10",
  "devDependencies": {
    "@biomejs/biome": "^2.4.6"
  }
}
